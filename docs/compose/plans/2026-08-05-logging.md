# 日志 + 一键上报 实现计划

> [!NOTE]
> This document may not reflect the current implementation.
> See the final report for up-to-date state:
> [Final Report](../reports/logging.md)

> **For agentic workers:** REQUIRED SUB-SKILL: Use compose:subagent (recommended) or compose:execute to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为剪贴板助手（v1.3.0）新增文件日志（关键事件/运行错误/未处理异常/捕获明细）与一键上报（错误弹窗 + 托盘入口 → POST 到自建服务器）。

**Architecture:** 客户端零依赖 `Log` 静态类写 `%LocalAppData%\ClipboardTool\logs\clipboard.log`（1MB 大小轮转 5 个文件），全局三钩子捕获未处理异常弹 `ErrorWindow`；上报时合并日志文件 POST 到 `https://code.starry0214.one/api/logs/upload`。服务器端在现有 Elysia (bun) 服务加 `logs.ts` 路由，落盘到 `/root/code-gen-service/logs/`。

**Tech Stack:** C# / .NET 9 WPF（客户端）、Elysia + bun（服务器端）、nginx（反代，需调大 client_max_body_size）。

## Global Constraints

- 所有 dotnet 命令在 `ClipboardTool/` 目录执行（shell workdir）；build 前杀 ClipboardTool 进程；build 输出用 `Select-String "error|个错误|个警告"` 检查完整输出，禁止截断。
- 项目无测试项目，验证方式 = build 通过 + 启动 exe 模拟操作检查行为/日志文件内容。
- 捕获明细**禁止记录剪贴板文本内容**，只记类型/大小/耗时（隐私决策，见 spec [S4]）。
- csproj 移除了 System.Diagnostics / System.Net.Http 全局隐式 using，用到需显式 using。
- 日志写失败静默忽略，不得影响主流程。
- 上报 body 上限 5MB；服务器端限制超限返回 413。
- SSH 服务器：`ssh -i ~/.ssh/id_ed25519 -p 1443 root@107.175.228.83`；服务目录 `/root/code-gen-service/`。
- 服务器服务进程是 `bun run src/index.ts`（当前 pid 879880），重启 = kill + nohup 重启，启动前先 `bun install` 如有新依赖（Elysia 已有，无需新依赖）。

---

### Task 1: 服务器端日志接收路由

**Covers:** [S7]

**Files:**
- Create: `/root/code-gen-service/src/routes/logs.ts`（服务器端，经 scp）
- Modify: `/root/code-gen-service/src/index.ts`（注册路由）
- Modify: 服务器 `/www/server/panel/vhost/nginx/code.starry0214.one.conf`（client_max_body_size）

**Interfaces:**
- Consumes: 无
- Produces: `POST https://code.starry0214.one/api/logs/upload`（body 为 text/plain，≤5MB，返回 200 "ok" / 413 "too large"）

- [ ] **Step 1: 本地创建 logs.ts 并通过 scp 上传**

本地 `ClipboardTool/.tools/` 下创建 `server_logs_route.ts`：

```typescript
import { Elysia } from "elysia";
import { mkdirSync, writeFileSync } from "node:fs";
import { randomBytes } from "node:crypto";

const LOG_DIR = "/root/code-gen-service/logs";
const MAX_BODY = 5 * 1024 * 1024;

export const logsRouter = new Elysia({ prefix: "/api/logs", maxBodySize: MAX_BODY })
  .post("/upload", async ({ request, set }) => {
    const text = await request.text();
    if (text.length > MAX_BODY) {
      set.status = 413;
      return "too large";
    }
    mkdirSync(LOG_DIR, { recursive: true });
    const ts = new Date().toISOString().replace(/[-:TZ.]/g, "").slice(0, 14);
    const name = `${ts}-${randomBytes(4).toString("hex")}.log`;
    writeFileSync(`${LOG_DIR}/${name}`, text);
    return "ok";
  });
```

上传并放置：
```bash
scp -i ~/.ssh/id_ed25519 -P 1443 .tools/server_logs_route.ts root@107.175.228.83:/root/code-gen-service/src/routes/logs.ts
```

- [ ] **Step 2: 修改 index.ts 注册路由**

在服务器上执行（index.ts 仅 26 行，在 `import { printRelayRouter }` 行后插入 import，在 `.use(printRelayRouter)` 后加 `.use(logsRouter)`）：

```bash
ssh -i ~/.ssh/id_ed25519 -p 1443 root@107.175.228.83 "cd /root/code-gen-service && sed -i 's|import { printRelayRouter } from \"./routes/printRelay\";|import { printRelayRouter } from \"./routes/printRelay\";\nimport { logsRouter } from \"./routes/logs\";|' src/index.ts && sed -i 's|\.use(printRelayRouter)|.use(printRelayRouter)\n  .use(logsRouter)|' src/index.ts && grep -n 'logsRouter' src/index.ts"
```

期望输出两行 grep 命中（import + .use）。

- [ ] **Step 3: nginx 调大 client_max_body_size**

在服务器 `/www/server/panel/vhost/nginx/code.starry0214.one.conf` 的 `server {` 块内（`server_name` 行后）插入 `client_max_body_size 10m;`：

```bash
ssh -i ~/.ssh/id_ed25519 -p 1443 root@107.175.228.83 "sed -i 's|server_name code.starry0214.one;|server_name code.starry0214.one;\n    client_max_body_size 10m;|' /www/server/panel/vhost/nginx/code.starry0214.one.conf && nginx -t && nginx -s reload && echo NGINX_OK"
```

期望输出 NGINX_OK。

- [ ] **Step 4: 重启 bun 服务并验证**

```bash
ssh -i ~/.ssh/id_ed25519 -p 1443 root@107.175.228.83 "pkill -f 'bun run src/index.ts'; sleep 1; cd /root/code-gen-service && nohup bun run src/index.ts > /tmp/code-gen-service.log 2>&1 & sleep 3; curl -s http://127.0.0.1:41713/api/logs/upload -X POST --data-binary 'test log line' && echo && ls /root/code-gen-service/logs/"
```

期望输出 `ok` 且 logs/ 目录出现一个新 `.log` 文件。再用公网验证：

```bash
curl -s -X POST --data-binary "hello from test" "https://code.starry0214.one/api/logs/upload" ; echo
```

期望输出 `ok`。

- [ ] **Step 5: 提交**

无 git 提交（服务器端改动不入本地仓库）。本地无文件改动需提交。

---

### Task 2: 客户端 Log 组件（写入/轮转/合并/上报）

**Covers:** [S3], [S6]

**Files:**
- Create: `ClipboardTool/Services/Log.cs`
- Test: 无（构建 + 启动验证）

**Interfaces:**
- Consumes: 无
- Produces: `Log.Init(string dataDir)`、`Log.Info(string msg)`、`Log.Error(string msg, Exception? ex = null)`、`Log.UploadAsync() : Task<bool>`、`Log.StartupTime`（供元信息）

- [ ] **Step 1: 创建 Log.cs**

```csharp
using System.Net.Http;
using System.Text;

namespace ClipboardTool;

/// <summary>
/// 轻量文件日志：%LocalAppData%\ClipboardTool\logs\clipboard.log，
/// 单文件 1MB 大小轮转（clipboard.log → .1.log … .4.log，共保留 5 个）。
/// 写失败静默忽略；上报时合并全部日志文件 POST 到更新服务器。
/// </summary>
public static class Log
{
    private static readonly object Lock = new();
    private static string _dir = "";
    private static readonly string UploadUrl = $"{Updater.UpdateBaseUrl.Replace("/updates", "")}/api/logs/upload";

    public static DateTime StartupTime { get; } = DateTime.Now;

    public static void Init(string dataDir)
    {
        lock (Lock)
        {
            _dir = Path.Combine(dataDir, "logs");
            try
            {
                Directory.CreateDirectory(_dir);
            }
            catch (Exception)
            {
                _dir = ""; // 无法建目录则禁用日志
            }
        }
    }

    public static void Info(string msg) => Write("INFO", msg, null);

    public static void Error(string msg, Exception? ex = null) => Write("ERROR", msg, ex);

    private static void Write(string level, string msg, Exception? ex)
    {
        lock (Lock)
        {
            if (string.IsNullOrEmpty(_dir))
                return;
            try
            {
                var path = Path.Combine(_dir, "clipboard.log");
                RollIfNeeded(path);
                var sb = new StringBuilder();
                sb.AppendLine($"{DateTime.Now:yyyy-MM-dd HH:mm:ss.fff} [{level}] {msg}");
                if (ex is not null)
                {
                    sb.AppendLine($"    {ex.GetType().Name}: {ex.Message}");
                    foreach (var line in (ex.StackTrace ?? "").Split('\n'))
                        if (!string.IsNullOrWhiteSpace(line))
                            sb.AppendLine($"    {line.Trim()}");
                }
                File.AppendAllText(path, sb.ToString(), Encoding.UTF8);
            }
            catch (Exception)
            {
                // 日志失败不影响主流程
            }
        }
    }

    private static void RollIfNeeded(string path)
    {
        var fi = new FileInfo(path);
        if (!fi.Exists || fi.Length < 1024 * 1024)
            return;
        // 最旧（.4.log）先删，然后逐级滚动
        File.Delete(Path.Combine(_dir, "clipboard.4.log"));
        for (var i = 3; i >= 1; i--)
        {
            var src = Path.Combine(_dir, $"clipboard.{i}.log");
            if (File.Exists(src))
                File.Move(src, Path.Combine(_dir, $"clipboard.{i + 1}.log"), true);
        }
        File.Move(path, Path.Combine(_dir, "clipboard.1.log"), true);
    }

    /// <summary>合并全部日志文件（头部附元信息），POST 上报。成功返回 true。</summary>
    public static async Task<bool> UploadAsync()
    {
        try
        {
            var sb = new StringBuilder();
            sb.AppendLine($"=== 剪贴板助手日志上报 {DateTime.Now:yyyy-MM-dd HH:mm:ss} ===");
            sb.AppendLine($"程序版本: {Updater.CurrentVersion}");
            sb.AppendLine($"系统: {Environment.OSVersion} / .NET {Environment.Version}");
            sb.AppendLine($"启动时间: {StartupTime:yyyy-MM-dd HH:mm:ss}");
            sb.AppendLine($"数据目录: {Path.Combine(Environment.GetFolderPath(Environment.SpecialFolder.LocalApplicationData), "ClipboardTool")}");
            sb.AppendLine();

            string[] files;
            lock (Lock)
            {
                if (string.IsNullOrEmpty(_dir) || !Directory.Exists(_dir))
                    return false;
                files = Directory.GetFiles(_dir, "clipboard*.log")
                    .OrderBy(f => f, StringComparer.Ordinal)
                    .ToArray();
            }
            foreach (var f in files)
            {
                try { sb.AppendLine($"----- {Path.GetFileName(f)} -----"); sb.Append(File.ReadAllText(f, Encoding.UTF8)); }
                catch (Exception) { }
            }

            using var http = new HttpClient { Timeout = TimeSpan.FromSeconds(30) };
            var content = new StringContent(sb.ToString(), Encoding.UTF8, "text/plain");
            using var resp = await http.PostAsync(UploadUrl, content);
            return resp.IsSuccessStatusCode;
        }
        catch (Exception)
        {
            return false;
        }
    }
}
```

- [ ] **Step 2: 构建验证**

```bash
Get-Process -Name ClipboardTool -ErrorAction SilentlyContinue | Stop-Process -Force
dotnet build 2>&1 | Select-String "error|个错误|个警告"
```

期望：`0 个错误`。注意 `Updater.UpdateBaseUrl` 为 `https://code.starry0214.one/updates`，`Replace("/updates","")` 后为 `https://code.starry0214.one`。

- [ ] **Step 3: 启动冒烟验证**

```bash
Start-Process "bin\Debug\net9.0-windows\ClipboardTool.exe"; Start-Sleep 4; Get-Process -Name ClipboardTool | Select-Object Id
```

期望：进程存在。日志文件此刻尚未生成（Init 未调用，Task 4 接入），此步只验证不崩溃。

- [ ] **Step 4: 提交**

```bash
git add ClipboardTool/Services/Log.cs
git commit -m "feat: 轻量文件日志组件（轮转+上报）"
```

---

### Task 3: ErrorWindow 错误/反馈窗口

**Covers:** [S6]

**Files:**
- Create: `ClipboardTool/ErrorWindow.xaml`
- Create: `ClipboardTool/ErrorWindow.xaml.cs`

**Interfaces:**
- Consumes: `Log.UploadAsync() : Task<bool>`
- Produces: `new ErrorWindow(string title, string message)`，公开方法 `ShowError(string title, string message)` / `ShowFeedback()`（静态便捷入口），内部上报按钮事件 `OnUploadClick`（上报成功/失败后 MessageBox 提示）

- [ ] **Step 1: 创建 ErrorWindow.xaml**

```xml
<Window x:Class="ClipboardTool.ErrorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="出错了"
        Width="480" SizeToContent="Height"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#F3F3F3"
        Icon="{StaticResource AppIcon}">
    <Border Style="{StaticResource CardBorder}" Margin="16">
        <StackPanel Margin="4">
            <TextBlock x:Name="TitleText" FontSize="15" FontWeight="SemiBold" Foreground="#1A1A1A" Margin="0,0,0,8"/>
            <TextBox x:Name="MessageBox"
                     IsReadOnly="True" TextWrapping="Wrap"
                     MaxHeight="180" VerticalScrollBarVisibility="Auto"
                     Background="#FAFAFA" BorderBrush="#E0E0E0" BorderThickness="1"
                     Padding="10" FontSize="12" Foreground="#444444"/>
            <TextBlock Text="点击"上报日志"可将日志发送给开发者（不包含剪贴板内容），便于排查问题。"
                       FontSize="11" Foreground="#888888" TextWrapping="Wrap" Margin="0,8,0,10"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="BtnClose" Content="关闭" Style="{StaticResource FluentButtonSecondary}"
                        Padding="16,6" Margin="0,0,8,0" Click="OnClose"/>
                <Button x:Name="BtnUpload" Content="上报日志" Style="{StaticResource FluentButton}"
                        Padding="16,6" Click="OnUpload"/>
            </StackPanel>
        </StackPanel>
    </Border>
</Window>
```

注意：XAML 属性值中嵌套双引号会破坏 XML——"上报日志"文案必须用 `&#x201C;`/`&#x201D;` 或单引号包裹的 XML 属性内单引号字符串。上一步如编译失败，将 TextBlock 文案改为：`Text="点击“上报日志”可将日志发送给开发者（不包含剪贴板内容），便于排查问题。"` 且外层用中文引号字符（“ ”）而非 ASCII 双引号。

- [ ] **Step 2: 创建 ErrorWindow.xaml.cs**

```csharp
using System.Windows;
using MessageBox = System.Windows.MessageBox;

namespace ClipboardTool;

/// <summary>错误/反馈窗口：显示错误信息，提供一键上报日志。</summary>
public partial class ErrorWindow : Window
{
    private readonly string _errorDetail;

    private ErrorWindow(string title, string message)
    {
        InitializeComponent();
        Title = title;
        TitleText.Text = title;
        _errorDetail = message;
        MessageBox.Text = message;
    }

    /// <summary>未处理异常弹窗（UI 线程）。</summary>
    public static void ShowError(string title, string message) =>
        new ErrorWindow(title, message).Show();

    /// <summary>托盘"反馈问题"入口：无错误上下文。</summary>
    public static void ShowFeedback() =>
        new ErrorWindow("反馈问题", "遇到了问题？点击"上报日志"把最近的日志发给开发者，方便定位问题。").Show();

    private async void OnUpload(object sender, RoutedEventArgs e)
    {
        BtnUpload.IsEnabled = false;
        BtnUpload.Content = "正在上报…";
        try
        {
            var ok = await Log.UploadAsync();
            MessageBox.Show(ok ? "日志已上报，感谢反馈！" : "上报失败，请检查网络后重试。",
                ok ? "上报成功" : "上报失败",
                MessageBoxButton.OK, ok ? MessageBoxImage.Information : MessageBoxImage.Warning);
        }
        finally
        {
            BtnUpload.IsEnabled = true;
            BtnUpload.Content = "上报日志";
        }
    }

    private void OnClose(object sender, RoutedEventArgs e) => Close();
}
```

注意：`ShowError` 用 `Show()`（非模态，避免阻塞主流程）；`_errorDetail` 字段当前仅存储，若后续需要"复制错误详情"按钮可用——按 YAGNI 若编译器报未使用字段警告则删除该字段与赋值。

- [ ] **Step 3: 构建验证**

```bash
Get-Process -Name ClipboardTool -ErrorAction SilentlyContinue | Stop-Process -Force
dotnet build 2>&1 | Select-String "error|个错误|个警告"
```

期望：`0 个错误`。

- [ ] **Step 4: 提交**

```bash
git add ClipboardTool/ErrorWindow.xaml ClipboardTool/ErrorWindow.xaml.cs
git commit -m "feat: 错误/反馈窗口（一键上报日志）"
```

---

### Task 4: 全局异常钩子 + 关键事件埋点 + --throw

**Covers:** [S5], [S4]

**Files:**
- Modify: `ClipboardTool/App.xaml.cs`

**Interfaces:**
- Consumes: `Log.Init/Info/Error`、`ErrorWindow.ShowError`
- Produces: 启动时挂三钩子；启动/退出/热键失败/更新结果/设置变更/数据迁移埋点；`--throw` 测试参数

- [ ] **Step 1: OnStartup 中初始化 Log 并挂钩子**

在 `App.xaml.cs` 的 `OnStartup` 中，`DataDir = dataDir;` 之后插入：

```csharp
        DataDir = dataDir;

        Log.Init(dataDir);
        Log.Info($"程序启动 v{Updater.CurrentVersion}，数据目录 {dataDir}");
        HookUnhandledExceptions();
```

并在类中新增方法（放在 OnStartup 之后）：

```csharp
    /// <summary>全局未处理异常捕获：UI 线程弹窗+记日志，其余线程记日志。</summary>
    private void HookUnhandledExceptions()
    {
        DispatcherUnhandledException += (_, e) =>
        {
            Log.Error("UI 线程未处理异常", e.Exception);
            e.Handled = true;
            ErrorWindow.ShowError("出错了", $"程序遇到未处理的错误：\n\n{e.Exception.Message}\n\n可以点击"上报日志"帮助排查。");
        };
        AppDomain.CurrentDomain.UnhandledException += (_, e) =>
            Log.Error("AppDomain 未处理异常", e.ExceptionObject as Exception);
        TaskScheduler.UnobservedTaskException += (_, e) =>
        {
            Log.Error("Task 未观察异常", e.Exception);
            e.SetObserved();
        };
    }
```

注意：lambda 内的中文引号同样避免 ASCII 双引号冲突（C# 字符串用中文引号“ ”字符即可，无转义问题）。

- [ ] **Step 2: 关键事件埋点**

在以下位置插入日志（App.xaml.cs 内）：

a) 数据迁移（`Directory.Move(legacyDir, dataDir)` 的 try 内成功后）：

```csharp
                Directory.Move(legacyDir, dataDir);
                Log.Info($"旧数据迁移成功: {legacyDir} → {dataDir}");
```

catch 块内：

```csharp
            catch (Exception ex)
            {
                dataDir = legacyDir; // 迁移失败回退旧位置，不丢数据
                Log.Error("旧数据迁移失败，回退旧目录", ex);
            }
```

b) 热键注册失败处（`RegisterHotkey` 的 catch 内，现有 `MessageBox.Show(...)` 前）：

```csharp
                Log.Error($"全局热键注册失败: {ex.Message}");
```

c) `CheckForUpdateAsync`：

- 方法开头 `var latest = await Updater.CheckAsync();` 前加 `var sw = System.Diagnostics.Stopwatch.StartNew();`
- `latest is null` 分支内：`Log.Error("检查更新失败：无法连接更新服务器");`
- `!Updater.IsNewer(latest)` 分支内：`Log.Info($"检查更新：当前已是最新 v{Updater.CurrentVersion}");`
- 下载前：`Log.Info($"开始下载更新 v{latest}，大小约 75MB");`
- `newExe is null` 分支内：`Log.Error("下载更新失败");`
- 下载成功后：`Log.Info($"下载完成 v{latest}，耗时 {sw.Elapsed.TotalSeconds:F1}s");`
- 下载窗口关闭后、`Updater.Apply` 前：`Log.Info("用户确认重启安装");`

d) `ApplySettings`（App.xaml.cs 内）开头：

```csharp
        Log.Info($"设置变更: MaxEntries={_settings.MaxEntries}, UseWinV={_settings.UseWinV}, Hotkey={_settings.HotkeyText}");
```

e) `OnExit`（`protected override void OnExit(ExitEventArgs e)` 内）开头：

```csharp
        Log.Info("程序退出");
```

- [ ] **Step 3: --throw 测试参数**

在 OnStartup 的测试钩子区域（`--show-overlay` 附近）插入：

```csharp
        // 测试钩子：--throw 模拟 UI 线程未处理异常（验证错误窗与日志）
        if (e.Args.Contains("--throw"))
            Dispatcher.BeginInvoke(() => throw new InvalidOperationException("测试异常：--throw 触发"));
```

- [ ] **Step 4: 构建验证**

```bash
Get-Process -Name ClipboardTool -ErrorAction SilentlyContinue | Stop-Process -Force
dotnet build 2>&1 | Select-String "error|个错误|个警告"
```

期望：`0 个错误`。

- [ ] **Step 5: 运行验证**

```bash
Remove-Item "$env:LOCALAPPDATA\ClipboardTool\logs" -Recurse -Force -ErrorAction SilentlyContinue
Start-Process "bin\Debug\net9.0-windows\ClipboardTool.exe"; Start-Sleep 4
Get-Content "$env:LOCALAPPDATA\ClipboardTool\logs\clipboard.log"
```

期望：含 `[INFO] 程序启动` 行。然后验证异常弹窗：

```bash
Get-Process -Name ClipboardTool | Stop-Process -Force
Start-Process "bin\Debug\net9.0-windows\ClipboardTool.exe" -ArgumentList "--throw"; Start-Sleep 4
Get-Content "$env:LOCALAPPDATA\ClipboardTool\logs\clipboard.log" | Select-String "ERROR"
```

期望：`[ERROR] UI 线程未处理异常` + 堆栈行，且屏幕出现"出错了"窗口（可随后关闭）。

- [ ] **Step 6: 提交**

```bash
git add ClipboardTool/App.xaml.cs
git commit -m "feat: 全局异常捕获与关键事件日志"
```

---

### Task 5: 捕获明细 + 运行中错误埋点

**Covers:** [S4]

**Files:**
- Modify: `ClipboardTool/Services/ClipboardMonitor.cs`
- Modify: `ClipboardTool/Services/Paster.cs`

**Interfaces:**
- Consumes: `Log.Info/Error`
- Produces: 无（纯埋点）

- [ ] **Step 1: ClipboardMonitor 捕获明细 + 错误日志**

修改 `OnClipboardUpdate` 的 catch：

```csharp
        catch (Exception ex)
        {
            // 剪贴板被其他进程锁定或格式无法读取时跳过本次
            Log.Error("剪贴板捕获失败", ex);
        }
```

修改 `Capture()`：在文本分支 `_store.Add(...)` 前记录：

```csharp
            var text = data.GetData(DataFormats.UnicodeText) as string;
            if (!string.IsNullOrEmpty(text))
            {
                _store.Add(new Entry { Type = "text", Content = text, CreatedAt = DateTimeOffset.UtcNow.ToUnixTimeSeconds() });
                Log.Info($"捕获文本 {text.Length} 字符");
            }
```

图片分支（`EncodePng(bmp)` 前记录尺寸；成功后记录大小）：

```csharp
                // 原图保存为 PNG 文件（Content 记录文件路径，粘贴纯文本时可给出路径）
                var png = EncodePng(bmp);
                var thumb = EncodePng(MakeThumb(bmp, 200));
                Log.Info($"捕获图片 {bmp.PixelWidth}x{bmp.PixelHeight}，PNG {png.Length / 1024}KB");
```

文件分支：

```csharp
                _store.Add(new Entry { Type = "file", Content = files[0], CreatedAt = DateTimeOffset.UtcNow.ToUnixTimeSeconds() });
                Log.Info($"捕获文件 {files[0]}");
```

注意：文件路径属常规信息（非剪贴板内容），允许记录。

- [ ] **Step 2: Paster 错误埋点**

修改 `Paste` 的 worker catch：

```csharp
            catch (Exception ex)
            {
                // 剪贴板持续被占用：复位抑制标志避免吞掉下一次捕获
                monitor.SuppressNext = false;
                Log.Error($"粘贴失败（类型 {entry.Type}）", ex);
            }
```

- [ ] **Step 3: 构建验证**

```bash
Get-Process -Name ClipboardTool -ErrorAction SilentlyContinue | Stop-Process -Force
dotnet build 2>&1 | Select-String "error|个错误|个警告"
```

期望：`0 个错误`。

- [ ] **Step 4: 运行验证**

```bash
Remove-Item "$env:LOCALAPPDATA\ClipboardTool\logs" -Recurse -Force -ErrorAction SilentlyContinue
Start-Process "bin\Debug\net9.0-windows\ClipboardTool.exe"; Start-Sleep 3
Set-Clipboard -Value "日志测试文本"; Start-Sleep 2
Get-Content "$env:LOCALAPPDATA\ClipboardTool\logs\clipboard.log" | Select-String "捕获"
```

期望：含 `[INFO] 捕获文本 5 字符`（“日志测试文本”5 个字符）且**不包含文本内容本身**（日志中不应出现"日志测试文本"字样，只有字符数）。

- [ ] **Step 5: 提交**

```bash
git add ClipboardTool/Services/ClipboardMonitor.cs ClipboardTool/Services/Paster.cs
git commit -m "feat: 剪贴板捕获明细与粘贴错误日志"
```

---

### Task 6: 托盘"反馈问题"入口

**Covers:** [S6]

**Files:**
- Modify: `ClipboardTool/Services/TrayIcon.cs`
- Modify: `ClipboardTool/App.xaml.cs`

**Interfaces:**
- Consumes: `ErrorWindow.ShowFeedback()`
- Produces: 托盘菜单项"反馈问题"

- [ ] **Step 1: TrayIcon 加菜单项与事件**

在 `TrayIcon.cs` 中，`public event Action? CheckUpdate;` 后加：

```csharp
    public event Action? Feedback;
```

在 `var miUpdate = new ToolStripMenuItem("检查更新");` 后加：

```csharp
        var miFeedback = new ToolStripMenuItem("反馈问题");
```

在 `miUpdate.Click += ...` 后加：

```csharp
        miFeedback.Click += (_, _) => Feedback?.Invoke();
```

在 `menu.Items.Add(miUpdate);` 后加：

```csharp
        menu.Items.Add(miFeedback);
```

- [ ] **Step 2: App.xaml.cs 接线**

在 `_tray.CheckUpdate += ...` 后加：

```csharp
        _tray.Feedback += ErrorWindow.ShowFeedback;
```

- [ ] **Step 3: 构建验证**

```bash
Get-Process -Name ClipboardTool -ErrorAction SilentlyContinue | Stop-Process -Force
dotnet build 2>&1 | Select-String "error|个错误|个警告"
```

期望：`0 个错误`。

- [ ] **Step 4: 运行验证**

启动程序后（无 --throw 参数），打开托盘菜单应出现"反馈问题"项；点击后弹出"反馈问题"窗口。手动确认后关闭。

- [ ] **Step 5: 提交**

```bash
git add ClipboardTool/Services/TrayIcon.cs ClipboardTool/App.xaml.cs
git commit -m "feat: 托盘反馈问题入口"
```

---

### Task 7: 端到端验证（上报链路）

**Covers:** [S8]

**Files:** 无（纯验证）

**Interfaces:**
- Consumes: Task 1-6 全部产物

- [ ] **Step 1: 触发异常并上报**

```bash
Get-Process -Name ClipboardTool -ErrorAction SilentlyContinue | Stop-Process -Force
Start-Process "bin\Debug\net9.0-windows\ClipboardTool.exe" -ArgumentList "--throw"; Start-Sleep 4
```

在"出错了"窗口点击"上报日志"（手动或 UI 自动化）。期望：提示"日志已上报，感谢反馈！"。

- [ ] **Step 2: 服务器确认落盘**

```bash
ssh -i ~/.ssh/id_ed25519 -p 1443 root@107.175.228.83 "ls -lt /root/code-gen-service/logs/ | head -3; echo '---'; tail -20 \$(ls -t /root/code-gen-service/logs/*.log | head -1)"
```

期望：最新 .log 文件含"剪贴板助手日志上报"头、程序版本、`[ERROR] UI 线程未处理异常`、`[INFO] 程序启动` 等行。

- [ ] **Step 3: 清理测试进程**

```bash
Get-Process -Name ClipboardTool -ErrorAction SilentlyContinue | Stop-Process -Force
```

- [ ] **Step 4: 提交（无改动）**

---

## Self-Review

**Spec coverage 检查：**
- [S3] 日志组件 → Task 2 ✓
- [S4] 记录内容四类 → Task 4（关键事件）+ Task 5（错误/明细）✓
- [S5] 未处理异常钩子 → Task 4 ✓
- [S6] 一键上报 → Task 2（UploadAsync）+ Task 3（ErrorWindow）+ Task 6（托盘入口）✓
- [S7] 服务器端 → Task 1 ✓
- [S8] 测试验证 → Task 4/5/7 ✓
- [S9] 范围外 → 无实现任务（正确）✓

**类型一致性：** `Log.Init/Info/Error/UploadAsync/StartupTime` 在 Task 2 定义，Task 3/4/5 使用一致；`ErrorWindow.ShowError/ShowFeedback` 在 Task 3 定义，Task 4/6 使用一致；`TrayIcon.Feedback` 事件 Task 6 定义并接线 ✓。

**已知风险：** ErrorWindow.xaml 中提示文案含 ASCII 双引号可能破坏 XML——计划中已给出替代方案（中文引号字符）。实施时以中文引号“ ”为准。

**占位符扫描：** 无 TBD/TODO；所有代码步骤含完整代码或精确 diff 描述。
