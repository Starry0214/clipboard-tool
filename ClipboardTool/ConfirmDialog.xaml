<Window x:Class="ClipboardTool.ConfirmDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="确认"
        Width="420" SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Icon="{StaticResource AppIcon}">
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="44" ResizeBorderThickness="0"
                            GlassFrameThickness="0" CornerRadius="0"
                            UseAeroCaptionButtons="False"/>
    </shell:WindowChrome.WindowChrome>
    <Border Background="{StaticResource CardBrush}"
            BorderBrush="#E8E8E8"
            BorderThickness="1" CornerRadius="14" Margin="10">
        <Border.Effect>
            <DropShadowEffect BlurRadius="28" ShadowDepth="4" Opacity="0.28" Color="#000000"/>
        </Border.Effect>
        <Grid>
            <!-- 标题栏拖拽区（WindowChrome CaptionHeight=44 已处理）与关闭按钮 -->
            <Button Content="&#xE8BB;" FontFamily="Segoe MDL2 Assets" Style="{StaticResource FluentButtonSecondary}"
                    Width="30" Height="30" Padding="0"
                    HorizontalAlignment="Right" VerticalAlignment="Top"
                    Margin="0,8,10,0" Click="OnCloseClick"
                    ToolTip="关闭"/>
            <StackPanel Margin="22,18,22,20">
                <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                    <!-- 警示/信息图标 -->
                    <Border x:Name="IconCircle" Width="44" Height="44" CornerRadius="22"
                            VerticalAlignment="Top">
                        <TextBlock x:Name="IconText"
                                   FontFamily="Segoe MDL2 Assets" FontSize="22"
                                   Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <StackPanel Margin="14,0,0,0" VerticalAlignment="Center">
                        <TextBlock x:Name="TitleText"
                                   FontSize="16" FontWeight="SemiBold"
                                   Foreground="{StaticResource TextPrimaryBrush}"/>
                        <TextBlock x:Name="SubtitleText" FontSize="12"
                                   Foreground="{StaticResource TextMutedBrush}" Margin="0,3,0,0"/>
                    </StackPanel>
                </StackPanel>
                <TextBlock x:Name="MessageText" FontSize="13" LineHeight="21"
                           Foreground="{StaticResource TextPrimaryBrush}"
                           TextWrapping="Wrap" Margin="0,0,0,20"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="取消" Style="{StaticResource FluentButtonSecondary}"
                            Padding="18,8" IsCancel="True"/>
                    <Button x:Name="ConfirmBtn" Padding="18,8" Margin="10,0,0,0"
                            Click="OnConfirm"/>
                </StackPanel>
            </StackPanel>
        </Grid>
    </Border>
</Window>
