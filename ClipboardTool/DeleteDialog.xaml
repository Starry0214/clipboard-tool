<Window x:Class="ClipboardTool.DeleteDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="删除"
        Width="380" SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Background="#F3F3F3"
        Icon="{StaticResource AppIcon}">
    <StackPanel Margin="20">
        <TextBlock Text="选择删除方式" FontWeight="SemiBold" Foreground="#1A1A1A" FontSize="14" Margin="0,0,0,8"/>
        <TextBlock x:Name="HintText" FontSize="12" Foreground="#666666" TextWrapping="Wrap" LineHeight="18" Margin="0,0,0,16"/>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="本地删除" Style="{StaticResource FluentButton}" Padding="14,6" Click="OnLocalDelete"/>
            <Button x:Name="FullDeleteBtn" Content="彻底删除" Style="{StaticResource FluentButtonSecondary}" Padding="14,6" Margin="8,0,0,0" Click="OnFullDelete"/>
            <Button Content="取消" Style="{StaticResource FluentButtonSecondary}" Padding="14,6" Margin="8,0,0,0" IsCancel="True"/>
        </StackPanel>
    </StackPanel>
</Window>
